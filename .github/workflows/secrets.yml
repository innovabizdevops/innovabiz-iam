version: 1.0

# Secrets globais
global:
  DOCKERHUB_USERNAME: ${DOCKERHUB_USERNAME}
  DOCKERHUB_TOKEN: ${DOCKERHUB_TOKEN}
  GITHUB_TOKEN: ${GITHUB_TOKEN}
  SONAR_TOKEN: ${SONAR_TOKEN}
  SNYK_TOKEN: ${SNYK_TOKEN}
  ZAP_API_KEY: ${ZAP_API_KEY}
  GRAFANA_PASSWORD: ${GRAFANA_PASSWORD}

# Secrets do IAM
iam:
  ADMIN_PASSWORD: ${IAM_ADMIN_PASSWORD}
  JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY}
  JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}

# Secrets do KrakenD
krakend:
  CONFIG_SECRET: ${KRAKEND_CONFIG_SECRET}
  MCP_SECRET: ${KRAKEND_MCP_SECRET}
  GRAPHQL_SECRET: ${KRAKEND_GRAPHQL_SECRET}

# Secrets do MCP
mcp:
  CORE_SECRET: ${MCP_CORE_SECRET}
  INTEGRATION_SECRET: ${MCP_INTEGRATION_SECRET}

# Secrets do GraphQL
graphql:
  FEDERATION_SECRET: ${GRAPHQL_FEDERATION_SECRET}
  SCHEMA_SECRET: ${GRAPHQL_SCHEMA_SECRET}

# Secrets do Observability
observability:
  PROMETHEUS_SECRET: ${PROMETHEUS_SECRET}
  GRAFANA_SECRET: ${GRAFANA_SECRET}
  ALERT_SECRET: ${ALERT_SECRET}

# Secrets do DevOps
devops:
  GITLAB_SECRET: ${GITLAB_SECRET}
  JENKINS_SECRET: ${JENKINS_SECRET}
  SONARQUBE_SECRET: ${SONARQUBE_SECRET}
  ZAP_SECRET: ${ZAP_SECRET}
  SNYK_SECRET: ${SNYK_SECRET}

# Secrets do CI/CD
ci_cd:
  WORKFLOW_SECRET: ${WORKFLOW_SECRET}
  TESTING_SECRET: ${TESTING_SECRET}
  DOCUMENTATION_SECRET: ${DOCUMENTATION_SECRET}
